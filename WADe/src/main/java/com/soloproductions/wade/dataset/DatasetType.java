package com.soloproductions.wade.dataset;

/**
 * High-level category that describes the shape of a dataset.
 */
public enum DatasetType
{
    /** Hierarchical cluster tree dataset. */
    CLUSTERS,

    /** Pairwise similarity heatmap dataset. */
    HEATMAP
}
