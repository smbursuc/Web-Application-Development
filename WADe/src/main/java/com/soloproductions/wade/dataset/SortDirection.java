package com.soloproductions.wade.dataset;

/**
 * Direction used when sorting dataset results.
 */
public enum SortDirection
{
    /** Sort in descending order. */
    DESCENDING,

    /** Sort in ascending order. */
    ASCENDING,

    /** No sorting is applied; results are returned in their natural order. */
    NO_SORT
}
